{
  "name": "mathilde-badufle-website",
  "private": true,
  "scripts": {
    "dev": "HUGO_CACHEDIR=\"$PWD/.hugo_cache\" hugo server -D",
    "build": "HUGO_CACHEDIR=\"$PWD/.hugo_cache\" hugo --gc --minify"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "@tailwindcss/cli": "^4.1.5",
    "tailwindcss": "^4.1.5"
  }
}
